# Operator scripts

Phase 1.1 includes documentation stubs only.

- Model download instructions arrive with Multilingual / LLM integration phases.
- Prefer `npm run doctor` / `uv run python -m app.scripts.doctor_stub` for a quick foundation check.
